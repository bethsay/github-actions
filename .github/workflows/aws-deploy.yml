# Sourced from https://github.com/actions/starter-workflows/blob/main/deployments/terraform.yml
# Sourced from https://github.com/warolv/github-actions-series/blob/master/.github/workflows/provision_ec2.yaml
# Sourced from https://github.com/spacelift-io-blog-posts/Blog-Technical-Content/blob/master/terraform-github-actions/.github/workflows/terraform.yml
name: 'Create S3 Bucket'

on:
  workflow_dispatch:
  # push:
  #   branches:
  #   - gha/deploy

jobs:
  aws-deploy:
    name: 'Create S3 Bucket in AWS with Terraform code'
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash
        working-directory: ./create-s3-bucket

    steps:
    - name: Clone the gha/deploy branch of current repo
      # Based on https://github.com/marketplace/actions/checkout
      uses: actions/checkout@v6
      # with:
      #   ref: 'gha/deploy'

    - name: Configure AWS credentials
      # Based on https://github.com/marketplace/actions/configure-aws-credentials-action-for-github-actions
      uses: aws-actions/configure-aws-credentials@v6
      with:
        aws-region: us-east-1
        aws-access-key-id: ${{ secrets.AWS_IAM_ACCESS_KEY }}
        aws-secret-access-key: ${{ secrets.AWS_IAM_SECRET_KEY }}

    - name: Install Terraform
      # Based on https://github.com/marketplace/actions/hashicorp-setup-terraform
      uses: hashicorp/setup-terraform@v3

    - name: Terraform Init
      run: terraform init

    - name: Terraform Plan
      run: terraform plan

    - name: Terraform Apply
      run: terraform apply -auto-approve
